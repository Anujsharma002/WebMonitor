version: '3.8'

services:
  webmonitor:
    build: .
    ports:
      - "5173:5173"
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./Backend/data.db:/app/Backend/data.db
      - ./Backend/.env:/app/Backend/.env
    restart: always
